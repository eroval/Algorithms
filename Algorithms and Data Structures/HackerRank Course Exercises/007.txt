Дадено е кореново дърво с  върха, номерирани от 1 до , представено със Списък на родителите. Като родител на корена е посочено цяло, коено е по-малко от 1. Напишете програма, която намира височината на дървото и височините на всеки от върховете.

English A rooted tree with  vertices, labeled from 1 to , is given, presented in ia List of parents. As a parent of the root an integer that is not a number of a vertex is given. Write a program to find the height of the rooted tree and the height of each vertex.

Input Format

На първия ред няа стандартния вход ще бъде зададен броят  на върховете на дървото, а на всеки от следващите  реда - по един от родителитне на върховете, подредени в нарастващ ред на номерата.

English On the first line of thwe standard input a number of the vertices  will bw givwn, and on each of the next  line the parent of a vertex, ordered in increasing order of labels.

Constraints


Output Format

На първия ред на стандартния изход програмата трябва да изведе височината на дървото, а на втория, разделени с по един интервал, височините на върховете, подредени в нарастващ ред на номерата им.

English On the first line of the standard output the program has to print the height of the rooted tree, and on the second - heights of the vertices, ordered in increasing order of their labels.

Sample Input 0

5
2
0
1
2
2
Sample Output 0

2
1 0 2 1 1 